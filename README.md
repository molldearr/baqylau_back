# baqylau_back

pip install redis
pip install celery
pip install apscheduler


sudo apt update

sudo apt install redis-server -y

sudo systemctl status redis

sudo systemctl enable redis

sudo systemctl redis-cli ping

